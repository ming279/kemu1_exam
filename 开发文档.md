# 驾照科目一系统 — 功能扩展开发文档

> 基于现有系统（核心考试 + 6 加分项）扩展教学管理与游戏化功能
> 身份设计：admin = 教师（已确认 ✓），user = 学生（不新增身份）
> 实现顺序：⑥ → ① → ② → ④ → ③ → ⑤ → ⑦

---

## 一、数据库变更总览

### 新增表

```sql
-- 教师发布的任务
CREATE TABLE task (
  id            INT AUTO_INCREMENT PRIMARY KEY,
  title         VARCHAR(200) NOT NULL,           -- 任务标题
  creator_uid   INT NOT NULL,                    -- 发布教师（user.id）
  qtype_filter  ENUM('all','judge','single') DEFAULT 'all',  -- 题型筛选
  question_count INT NOT NULL DEFAULT 100,       -- 题目数量
  judge_count   INT NOT NULL DEFAULT 40,         -- 判断题数量
  single_count  INT NOT NULL DEFAULT 60,         -- 单选题数量
  time_limit_sec INT,                            -- 时限秒数（NULL=不限时，教师输入分钟数×60）
  mode          ENUM('exam','practice') NOT NULL, -- 考试/练习
  purpose       ENUM('exam','review') DEFAULT 'exam', -- 用途：考试（防作弊打乱）/讲解（不打乱）
  question_ids  TEXT,                            -- 预生成的题目ID列表（逗号分隔）
  shuffle_order BOOLEAN DEFAULT FALSE,           -- 是否打乱顺序（purpose=exam 时可勾选）
  status        ENUM('draft','published','closed') DEFAULT 'published',
  created_at    DATETIME DEFAULT CURRENT_TIMESTAMP,
  closed_at     DATETIME,                       -- 关闭时间
  FOREIGN KEY (creator_uid) REFERENCES user(id)
);

-- 学生参加任务的记录
CREATE TABLE task_record (
  id            INT AUTO_INCREMENT PRIMARY KEY,
  task_id       INT NOT NULL,
  uid           INT NOT NULL,                    -- 学生
  paper_id      INT,                             -- 关联 exam_paper.id
  status        ENUM('not_started','in_progress','completed','expired') DEFAULT 'not_started',
  start_time    DATETIME,                        -- 开始作答时间
  submit_time   DATETIME,                        -- 交卷时间
  elapsed_sec   INT DEFAULT 0,                   -- 实际用时（练习模式含暂停）
  score         DECIMAL(5,2),                    -- 得分
  paused        BOOLEAN DEFAULT FALSE,           -- 练习模式是否暂停中
  pause_time    DATETIME,                        -- 暂停时刻
  UNIQUE KEY uk_task_user (task_id, uid),        -- 每人每任务一条
  FOREIGN KEY (task_id) REFERENCES task(id),
  FOREIGN KEY (uid) REFERENCES user(id),
  FOREIGN KEY (paper_id) REFERENCES exam_paper(id)
);

-- 双人PK对战（功能⑦，最后做）
CREATE TABLE pk_challenge (
  id            INT AUTO_INCREMENT PRIMARY KEY,
  challenger_uid INT NOT NULL,                   -- 发起方
  opponent_uid  INT NOT NULL,                   -- 被挑战方
  question_ids  VARCHAR(500) NOT NULL,           -- 10题ID逗号分隔
  status        ENUM('pending','challenger_done','opponent_done','completed','declined') DEFAULT 'pending',
  challenger_score  DECIMAL(5,2),
  opponent_score   DECIMAL(5,2),
  winner_uid   INT,
  created_at   DATETIME DEFAULT CURRENT_TIMESTAMP,
  completed_at DATETIME,
  FOREIGN KEY (challenger_uid) REFERENCES user(id),
  FOREIGN KEY (opponent_uid) REFERENCES user(id)
);
```

### 修改表

```sql
-- 功能⑥：题目加答案解析
ALTER TABLE question ADD COLUMN explanation TEXT COMMENT '答案解析（学生交卷后可见）';

-- exam_paper 关联任务（NULL=默认模拟考试，非空=教师任务）
ALTER TABLE exam_paper ADD COLUMN task_id INT;
```

---

## 二、功能⑥：答案解析（第一步做，改动最小）

### 目标
- 教师给题目设置答案解析
- 学生交卷后看到每题的解析

### 后端改动

#### 2.1 数据库迁移
```sql
ALTER TABLE question ADD COLUMN explanation TEXT;
```

#### 2.2 新增路由（main.py）

| 路由 | 方法 | 功能 |
|---|---|---|
| `/admin/questions` | GET | 题目管理页（分页列表，每题可编辑解析） |
| `/admin/questions/<qid>/explanation` | POST | 保存单题解析（AJAX） |

#### 2.3 模板改动

**新增 `admin_questions.html`**：
- 分页表格：题号、题干摘要、题型、答案、解析（可编辑 textarea）
- 每题"保存解析"按钮，AJAX POST 提交

**修改 `exam_result.html`**：
- 每题下方追加 `<div class="explanation">答案解析：{{ q.explanation }}</div>`
- 仅交卷后展示（当前 exam_result 就是交卷后页面，无需额外控制）

**修改 `practice_summary.html`**（如有）：
- 同上，练习结束后展示解析

### 数据来源决策
- **方案 C（已确认 ✓）**：先建功能框架（字段→编辑页面→学生端展示），解析内容留空，之后可手动编辑或批量导入

### 验收标准
- [ ] 管理员能进入题目管理页
- [ ] 能编辑任意题的解析并保存
- [ ] 学生交卷后能在每题下方看到解析
- [ ] 考试进行中不显示解析
- [ ] 无解析的题显示"暂无解析"（不报错）

---

## 三、功能①：教师发布任务（核心管理功能）

### 目标
- 教师创建任务：选题目数量、题型、时限、模式（考试/练习）
- 学生首页看到待完成任务

### 后端改动

#### 3.1 新增路由

| 路由 | 方法 | 功能 |
|---|---|---|
| `/admin/tasks` | GET | 任务管理页（列表 + 新建表单） |
| `/admin/tasks/create` | POST | 创建任务 |
| `/admin/tasks/<tid>/close` | POST | 关闭任务 |
| `/admin/tasks/<tid>/delete` | POST | 删除任务（软删除→status=closed） |
| `/admin/tasks/<tid>/edit` | POST | 修改任务标题/时限（题目不可改，已确认 ✓） |

#### 3.2 组卷逻辑
```python
# 教师发布任务时预生成一套固定题目列表（存 question_ids 字段）
# 学生进入时读取该列表，按 purpose 决定是否打乱

def _generate_questions_for_task(judge_count, single_count):
    """教师发布时调用：分别抽判断题和单选题，合并存 question_ids"""
    judges = SELECT * FROM question WHERE qtype='judge' ORDER BY RAND() LIMIT judge_count
    singles = SELECT * FROM question WHERE qtype='single' ORDER BY RAND() LIMIT single_count
    # 判断题在前，单选题在后（不打乱时分区展示，打乱时各区内打乱）
    return [q.id for q in judges] + [q.id for q in singles]

def _get_questions_for_student(task, uid):
    """学生进入任务时调用：读 question_ids，按 purpose 决定顺序"""
    ids = task['question_ids'].split(',')
    judges = [id for id in ids if get_qtype(id) == 'judge']
    singles = [id for id in ids if get_qtype(id) == 'single']
    if task['purpose'] == 'exam' and task['shuffle_order']:
        # 用 uid 做种子打乱，同一学生顺序一致，不同学生不同
        import random
        rng = random.Random(uid)
        rng.shuffle(judges)
        rng.shuffle(singles)
    # 判断题始终在前，单选题在后（不混在一起）
    return judges + singles
```

#### 3.2.1 组卷规则（已确认 ✓）
- **同一套题**：教师发布时预生成固定题目列表，所有学生题目内容相同
- **用途选择**：
  - `purpose=exam`（考试/防作弊）：可选"打乱顺序"，判断题和单选题各自打乱但不混在一起
  - `purpose=review`（讲解/练习）：不打乱，全班顺序一致，方便逐题讲评
- **题型不混合**：判断题始终在前（1~N），单选题在后（N+1~M），打乱只在各自区内打乱
- **选项顺序不打乱**：保证"A/B/C/D"可对齐，便于讲评

#### 3.3 模板改动

**新增 `admin_tasks.html`**：
- 已发布任务列表表（标题、模式、题数、时限、状态、已参加人数、操作）
- 新建任务表单：标题、题型下拉、题数输入、时限输入（留空=不限）、模式单选

**修改 `index.html`（学生首页）**：
- 新增"我的任务"区块，列出未完成的 published 任务
- 点击任务 → 进入作答（路由 `/task/<tid>/start`）

### 验收标准
- [ ] 教师能创建任务并发布
- [ ] 学生首页看到待完成任务
- [ ] 点击任务能进入作答（复用现有考试/练习模板）
- [ ] 教师能关闭/删除任务

---

## 四、功能②：学生参加任务（考试时限/练习自主计时）

### 目标
- 考试模式：强制倒计时，到点自动交卷
- 练习模式：自主开始、暂停、继续

### 后端改动

#### 4.1 新增路由

| 路由 | 方法 | 功能 |
|---|---|---|
| `/task/<tid>/start` | POST | 开始任务（建 task_record + exam_paper） |
| `/task/<tid>/pause` | POST | 暂停（仅练习模式） |
| `/task/<tid>/resume` | POST | 继续（仅练习模式） |
| `/task/<tid>/submit` | POST | 交卷（复用现有判分逻辑） |

#### 4.2 计时逻辑（已确认 ✓ 后端记录）

**考试模式**：
```javascript
// exam.html 模板里加倒计时
var remain = {{ task.time_limit_sec }};  // 后端传剩余秒数
var timer = setInterval(function() {
    remain--;
    $('#countdown').text(formatTime(remain));
    if (remain <= 0) {
        clearInterval(timer);
        $('#exam-form').submit();  // 到点自动交卷
    }
}, 1000);
```

**练习模式（后端记录计时）**：
```python
# 开始：记录 start_time，status=in_progress
POST /task/<tid>/start
  → task_record.start_time = NOW(), status = 'in_progress'

# 暂停：记录 pause_time，paused=True
POST /task/<tid>/pause
  → task_record.pause_time = NOW(), paused = True
  → 累加 elapsed_sec += NOW() - 上次resume_time

# 继续：记录 resume_time，paused=False
POST /task/<tid>/resume
  → task_record.pause_time = NULL, paused = False
  → 后续暂停时再累加

# 交卷：最终累加 elapsed_sec
POST /task/<tid>/submit
  → if not paused: elapsed_sec += NOW() - start_time - 已累计的暂停时段
  → status = 'completed'
```

**优势**：刷新页面/网络波动不丢失计时，从后端恢复计时状态

#### 4.3 模板改动

**修改 `exam.html`**：
- 顶部加倒计时显示（仅考试模式且 time_limit_sec 不为空）
- 到点自动提交表单

**修改 `practice.html`**（或新建 `task_practice.html`）：
- 加"开始计时/暂停/继续"按钮组
- 显示已用时间

### 验收标准
- [ ] 考试模式有倒计时，到点自动交卷
- [ ] 练习模式可开始/暂停/继续
- [ ] 暂停后刷新页面能恢复计时状态
- [ ] 交卷后 task_record.status = completed

---

## 五、功能④：错题排行榜（SQL 聚合）

### 目标
- 统计哪些题错最多，方便教师讲解

### 后端改动

#### 5.1 新增路由

| 路由 | 方法 | 功能 |
|---|---|---|
| `/admin/wrong_rank` | GET | 错题排行榜页 |

#### 5.2 核心查询
```sql
SELECT q.id, q.stem, q.qtype, q.answer,
       COUNT(*) AS wrong_count,
       COUNT(DISTINCT ed.paper_id) AS total_attempts,
       ROUND(COUNT(*) * 100.0 / COUNT(DISTINCT ed.paper_id), 1) AS wrong_rate
FROM exam_detail ed
JOIN question q ON q.id = ed.question_id
WHERE ed.is_correct = 0
GROUP BY q.id
ORDER BY wrong_count DESC
LIMIT 50;
```

#### 5.3 模板改动

**新增 `admin_wrong_rank.html`**：
- 表格：排名、题号、题干摘要、题型、正确答案、错次、错误率、常见错误答案
- 每题"去讲解"链接（跳转题目管理页定位该题）
- 支持按任务/时间范围筛选

### 展示范围（已确认 ✓）
- **默认全局**：所有 exam_detail 汇总，看整体薄弱点
- **可按任务筛选**：下拉选某次任务，看单次讲评重点
- 两种模式同一页面，切换下拉即更新

### 验收标准
- [ ] 排行榜显示错次最多的 50 题
- [ ] 有错误率百分比
- [ ] 可按任务筛选
- [ ] 全局/按任务切换正常

---

## 六、功能③：查看学生答题数据（可导出 CSV）

### 目标
- 管理端查看所有学生答题明细，可导出 CSV

### 后端改动

#### 6.1 新增路由

| 路由 | 方法 | 功能 |
|---|---|---|
| `/admin/answer_data` | GET | 答题数据页（筛选+表格） |
| `/admin/answer_data/export` | GET | 导出 CSV |

#### 6.2 CSV 导出
```python
from flask import Response
import csv, io

@app.route('/admin/answer_data/export')
@admin_required
def export_answer_data():
    # 查询 exam_detail + user + question
    output = io.StringIO()
    writer = csv.writer(output)
    writer.writerow(['学生', '题号', '题型', '题干', '学生答案', '正确答案', '对错', '用时', '提交时间'])
    for row in rows:
        writer.writerow([...])
    return Response(
        '\ufeff' + output.getvalue(),  # BOM 头让 Excel 识别 UTF-8
        mimetype='text/csv',
        headers={'Content-Disposition': 'attachment; filename=answer_data.csv'}
    )
```

### 导出格式（已确认 ✓）
- **CSV + Excel 两种**：CSV 通用兜底，Excel 带格式（表头加粗、错题行标红、自动列宽）
- 技术依赖：openpyxl 库（已在 requirements.txt）

### 导出内容维度（已确认 ✓ 场景驱动，5 种全选）

| 场景 | 教师用途 | 导出列 | 文件名示例 |
|---|---|---|---|
| 1. 课堂讲评 | 拿打印纸逐题讲 | 题号、题干、题型、正确答案、全班错误率、常见错误答案、错的人名 | `讲评单_任务A.xlsx` |
| 2. 成绩公告 | 贴墙/发群 | 排名、姓名、分数、通过/未通过、用时 | `成绩公告_任务A.xlsx` |
| 3. 个别辅导 | 找某学生谈心 | 题号、题干、他的答案、正确答案、对错、用时 | `学生张三_答题明细.xlsx` |
| 4. 教学反思 | 分析薄弱知识点 | 分类名、该分类题数、平均错误率、错最多3题 | `教学反思_全局.xlsx` |
| 5. 全量存档 | 所有原始数据 | 时间、学生、任务、题号、答案、对错、用时 | `全量明细_2026-09.xlsx` |

### 导出面板设计
```
场景选择（单选）：
  ○ 1. 课堂讲评（按题号，含错误率+错的人名）
  ○ 2. 成绩公告（按排名，含分数+通过/用时）
  ○ 3. 个别辅导（单学生全部答题明细）
  ○ 4. 教学反思（按分类聚合错误率）
  ○ 5. 全量存档（所有原始数据）

筛选条件：任务[下拉] 学生[下拉] 时间[起~止] 题型[下拉]
格式：[导出CSV]  [导出Excel]
```

### 验收标准
- [ ] 筛选条件生效（学生/任务/时间）
- [ ] CSV 下载后 Excel 打开中文正常
- [ ] Excel 带格式（表头加粗、错题标红、列宽自适应）
- [ ] 表格分页显示

---

## 七、功能⑤：学生排名（练习+考试分开）

### 目标
- 学生看自己在考试/练习中的排名
- 教师看全部排名

### 后端改动

#### 7.1 新增路由

| 路由 | 方法 | 功能 |
|---|---|---|
| `/ranking` | GET | 排名页（学生看自己附近，教师看全部） |

#### 7.2 排名查询
```sql
-- 考试排名：按平均分
SELECT u.id, u.username,
       COUNT(ep.id) AS exam_count,
       AVG(ep.score) AS avg_score,
       SUM(ep.passed) AS pass_count
FROM user u LEFT JOIN exam_paper ep ON ep.uid = u.id
WHERE u.role = 'user'
GROUP BY u.id
ORDER BY avg_score DESC;

-- 练习排名：按总答题数+正确率
SELECT u.id, u.username,
       COUNT(pr.id) AS practice_count,
       SUM(pr.is_correct) AS correct_count,
       ROUND(SUM(pr.is_correct)*100.0/COUNT(pr.id), 1) AS practice_rate
FROM user u LEFT JOIN practice pr ON pr.uid = u.id
WHERE u.role = 'user'
GROUP BY u.id
ORDER BY correct_count DESC;
```

### 展示方式（已确认 ✓）
- **表格 + 段位徽章 + 前三名奖牌**
- 前三名显示 🥇🥈🥉 特殊样式
- 段位徽章与 PK 共用（青铜/白银/黄金/铂金）
- 学生端只看自己排名 + 前后各 5 名（隐私保护）
- 教师端看全部

### 排名维度（已确认 ✓）
| 维度 | 排序依据 |
|---|---|
| 考试排名 | 按平均分降序 |
| 练习排名 | 按总正确题数降序（鼓励多练） |

### 验收标准
- [ ] 学生端只看自己排名+前后5名
- [ ] 教师端看全部
- [ ] 考试和练习排名分开
- [ ] 前三名奖牌样式正常
- [ ] 段位徽章显示正常
- [ ] 学生可看到其他学生的具体成绩（全部可见，已确认 ✓）

---

## 八、功能⑦：双人实时同步 PK（赛车竞速游戏）

### 已确认决策 ✓
- **技术架构**：flask-socketio（WebSocket 双向通信）
- **网络方案**：同一局域网（Flask 绑 0.0.0.0:5000）
- **答题模式**：同题同步（双方同时答同一题）
- **游戏化元素**：全选（进度条+比分+反馈动画+抢答+倒计时+表情包+段位徽章+小车竞速）
- **小车竞速**：左右双车道，得分越高车越靠前，开场冲出+最后一题冲刺+赢者过终点烟花

### 目标
- 两人同时在线，同题同步答题，赛车竞速可视化，全程动画反馈

### 技术依赖
```
flask-socketio    # WebSocket 双向通信
eventlet           # 异步 WSGI 服务器
```

### 启动方式变更
```python
# app/main.py
from flask_socketio import SocketIO
socketio = SocketIO(app, cors_allowed_origins="*")

if __name__ == '__main__':
    socketio.run(app, host='0.0.0.0', port=5000, debug=True)
```

### 数据库变更
```sql
-- pk_challenge 表（改版）
CREATE TABLE pk_challenge (
  id              INT AUTO_INCREMENT PRIMARY KEY,
  challenger_uid  INT NOT NULL,
  opponent_uid    INT NOT NULL,
  question_ids    VARCHAR(500) NOT NULL,         -- 10题ID逗号分隔
  status          ENUM('waiting','ready','playing','finished','declined') DEFAULT 'waiting',
  current_q       INT DEFAULT 0,                 -- 当前第几题（0-9）
  challenger_score DECIMAL(5,2) DEFAULT 0,
  opponent_score  DECIMAL(5,2) DEFAULT 0,
  challenger_answer VARCHAR(20),                 -- 每题答题记录
  opponent_answer  VARCHAR(20),
  winner_uid      INT,
  created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
  finished_at     DATETIME,
  FOREIGN KEY (challenger_uid) REFERENCES user(id),
  FOREIGN KEY (opponent_uid) REFERENCES user(id)
);

-- 用户表加段位字段
ALTER TABLE user ADD COLUMN pk_wins INT DEFAULT 0;
ALTER TABLE user ADD COLUMN pk_losses INT DEFAULT 0;
ALTER TABLE user ADD COLUMN win_streak INT DEFAULT 0;
```

### 路由设计

| 路由 | 方法 | 功能 |
|---|---|---|
| `/pk` | GET | PK 大厅（在线用户列表+我的对战记录+段位） |
| `/pk/challenge` | POST | 发起挑战（选对手 uid） |
| `/pk/<pid>` | GET | PK 房间页（等待准备/答题/结果，同一页面不同状态） |

### WebSocket 事件

| 事件 | 方向 | 说明 |
|---|---|---|
| `join_room` | 客户端→服务端 | 加入PK房间 |
| `ready` | 客户端→服务端 | 玩家准备就绪 |
| `start_game` | 服务端→客户端 | 双方准备完毕，开始倒计时 |
| `answer` | 客户端→服务端 | 提交本题答案 |
| `score_update` | 服务端→客户端 | 推送双方得分+当前题号（更新小车位置） |
| `next_question` | 服务端→客户端 | 推送下一题内容 |
| `opponent_answered` | 服务端→客户端 | 对方已答题（抢答锁题） |
| `game_over` | 服务端→客户端 | 游戏结束，推送胜负+最终得分 |
| `emoji` | 客户端→服务端→对方 | 发送快捷表情 |
| `opponent_left` | 服务端→客户端 | 对方掉线 |

### 游戏流程
```
1. A 在 PK 大厅选对手 B → 创建 pk_challenge(status=waiting)
2. B 收到邀请弹窗 → 接受 → status=ready
3. 双方进入 PK 房间 → 都点"准备" → status=playing
4. 全屏倒计时 3-2-1-GO! → 两车从起点冲出
5. 同题同步答 10 题：
   - 每题 15 秒倒计时（圆环动画）
   - 答对：车前进 10%，绿色闪光，+1 分
   - 答错：车不动，红色震动
   - 抢答：先答对的人锁定该题，对方变灰不能答
   - 超时：双方都 0 分，进下一题
   - 最后一题：赛道冲刺模式（加速动画）
6. 结果页：
   - 赢方车冲过终点 → 烟花 🎆 + "胜利！" 弹入
   - 输方车停住 → "惜败" 淡入
   - 得分对比 + 答题回顾
   - 段位更新（胜场+1，连胜+1，升级动画）
```

### 页面布局（竖向赛道，左侧赛车右侧题目）
```
┌──────────────┬───────────────────────────┐
│              │ ⚔️ 第 7/10 题  ⏱ 12秒     │
│  🏁 终点     │ 我 3 : 2 对手             │
│  ━━━━━━━━━  │                           │
│       🚗    │ 驾驶机动车在道路上违反      │
│  ━━━━━━━━━  │ 道路交通安全法规的行为...  │
│   (我 60%)  │                           │
│              │ A. 正确                    │
│  ━━━━━━━━━  │ B. 错误                    │
│              │                           │
│    🚙       │                           │
│  ━━━━━━━━━  ├───────────────────────────┤
│  (对手 40%) │ 😊加油 😂哈哈 😤不服 👍牛  │
│              │                           │
│  ▓ 起点线    │                           │
└──────────────┴───────────────────────────┘

- 左侧：两条竖向赛道，车从下往上跑
- 车位置 = bottom: (得分/总题数) × 90%
- 终点在顶部，起点在底部
- 右侧：题目+选项+表情包
- 左右各占约 40% / 60% 宽度
```

### 动画清单

| 时刻 | 动画 | 技术 |
|---|---|---|
| 开场 | 3-2-1-GO 全屏倒计时，车从底部起点冲出 | CSS keyframes |
| 答对 | 车向上前进 10% + 绿色闪光 + ✓ 弹出 | CSS transition + animation |
| 答错 | 车不动 + 红色震动 + ✗ 弹出 | CSS shake animation |
| 抢答 | 对方屏幕该题变灰，不可选 | WebSocket 推送 + CSS |
| 倒计时 | 圆环从满到空，颜色绿→黄→红 | SVG / CSS conic-gradient |
| 最后一题 | 终点线放大，车加速向上冲刺 | CSS 加速 transition |
| 过终点 | 赢方车冲过顶部终点线，烟花爆炸 + "胜利"/"惜败" 文字 | CSS particles + animation |
| 表情包 | 右下角气泡弹出 1 秒消失 | CSS 弹出动画 |
| 段位升级 | 徽章弹入 + 连胜火焰 | CSS animation |

### 段位系统
```
胜场 0-4:  🥉 青铜
胜场 5-14: 🥈 白银
胜场 15-29:🥇 黄金
胜场 30+:  💎 铂金

连胜 3 场:  名字旁 🔥
连胜 5 场:  进入时火焰边框 0.5 秒
```

### 验收标准
- [ ] 两台电脑同一局域网能同时访问
- [ ] A 发起挑战，B 收到邀请
- [ ] 双方准备后倒计时开始
- [ ] 同题同步答题，双方看到同一题
- [ ] 答对小车前进，答错不动
- [ ] 抢答机制生效（先答对锁题）
- [ ] 倒计时圆环动画正常
- [ ] 最后一题冲刺动画
- [ ] 赢方过终点烟花 + "胜利"
- [ ] 段位徽章在名字旁显示
- [ ] 表情包发送接收正常
- [ ] 掉线时对方收到提示

---

## 九、导航栏变更预览

### 教师端
```
首页 | 模拟考试 | 顺序练习 | 错题本 | 我的统计
     | 全局统计 | 题目管理 | 任务管理 | 错题排行 | 答题数据
     | 题库采集 | AI 验证 | 排名 | 退出
```

### 学生端
```
首页 | 模拟考试 | 顺序练习 | 错题本 | 我的统计 | 我的任务 | 排名 | PK挑战 | 退出
```

---

## 十、文件变更清单

| 文件 | 操作 | 涉及功能 |
|---|---|---|
| `sql/schema.sql` | 修改 | 新增 task/task_record/pk_challenge 表，question 加 explanation |
| `app/main.py` | 修改 | 新增约 15 个路由 |
| `app/templates/admin_questions.html` | 新增 | ⑥ |
| `app/templates/admin_tasks.html` | 新增 | ① |
| `app/templates/admin_wrong_rank.html` | 新增 | ④ |
| `app/templates/admin_answer_data.html` | 新增 | ③ |
| `app/templates/ranking.html` | 新增 | ⑤ |
| `app/templates/pk.html` | 新增 | ⑦ |
| `app/templates/index.html` | 修改 | 学生首页加"我的任务" |
| `app/templates/exam.html` | 修改 | 倒计时 |
| `app/templates/exam_result.html` | 修改 | 显示解析 |
| `app/templates/base.html` | 修改 | 导航栏新增项 |
| `app/static/style.css` | 修改 | 新增样式 |

---

## 十一、开发步骤检查表

按顺序执行，每步完成后打勾：

- [ ] 步骤1：数据库迁移（加 explanation 字段 + 建 task/task_record 表）
- [ ] 步骤2：功能⑥ 答案解析（管理页+展示）
- [ ] 步骤3：功能① 教师发布任务（表+页面+路由）
- [ ] 步骤4：功能② 学生参加任务（计时+暂停+交卷）
- [ ] 步骤5：功能④ 错题排行榜
- [ ] 步骤6：功能③ 答题数据导出
- [ ] 步骤7：功能⑤ 学生排名
- [ ] 步骤8：功能⑦ 双人PK
- [ ] 步骤9：导航栏+样式统一
- [ ] 步骤10：全流程测试+提交GitHub
