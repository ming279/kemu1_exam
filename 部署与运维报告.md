# 驾照科目一系统 — 部署与运维报告

> 项目：Flask + MySQL 8.0 驾照科目一题库管理与模拟考试系统
> 服务器：134.175.20.95（腾讯云 Ubuntu 22.04）
> 代码仓库：https://github.com/ming279/kemu1_exam
> 报告日期：2026-09-10

---

## 一、服务器环境概览

| 项目 | 配置 | 说明 |
|---|---|---|
| 云平台 | 腾讯云轻量服务器 | Ubuntu 22.04 LTS |
| 公网 IP | 134.175.20.95 | |
| Web 框架 | Flask 3.0 + Flask-SocketIO 5.3 | SocketIO 用于 PK 对战实时通信 |
| WSGI 服务器 | Gunicorn + Gevent | 协程支持 WebSocket 长连接 |
| 数据库 | MySQL 8.0 | max_allowed_packet=512M（备份含图片 BLOB） |
| 反向代理 | Nginx | 监听 80 端口，反代到内部 5000 |
| 进程管理 | systemd | 崩溃自动重启，开机自启 |

### 架构拓扑

```
校园网用户浏览器
    │
    ▼ HTTP :80
┌──────────┐
│  Nginx   │  反向代理 + WebSocket 升级
└────┬─────┘
     │ proxy_pass http://127.0.0.1:5000
     ▼
┌──────────────────┐
│  Gunicorn+Gevent │  Flask 应用 (:5000)
│  (systemd 托管)   │
└────┬─────────────┘
     │
     ▼ TCP 3306
┌──────────┐
│ MySQL 8.0│
└──────────┘
```

---

## 二、部署与运维中遇到的问题及解决过程

以下按时间顺序记录部署和运维过程中遇到的实际问题、分析过程和最终解决方案。

### 问题 1：校园网防火墙拦截非标准端口，用户无法访问系统

**问题现象**

系统部署在 5000 端口，在非校园网环境（如手机热点）下访问 `http://134.175.20.95:5000/` 一切正常，但切换到校园网后浏览器提示无法访问，连接超时。所有校园网用户均无法使用系统。

**分析过程**

按以下步骤逐一排查，逐步缩小问题范围：

| 步骤 | 操作 | 结果 | 排除的可能性 |
|---|---|---|---|
| 1 | SSH 登录服务器执行 `curl http://127.0.0.1:5000/login` | 返回 HTTP 200 | 排除"服务未启动" |
| 2 | 检查腾讯云控制台安全组入站规则 | TCP 5000 已放行 | 排除"云平台安全组拦截" |
| 3 | 手机热点访问 `:5000` | 正常打开 | 排除"服务端配置问题" |
| 4 | 校园网下分别测试 `:5000` 和 `:80` | 5000 超时，80 可通 | 定位到"校园网出口防火墙拦截非标准端口" |

**根因**

校园网边界防火墙采用**默认拒绝（Default Deny）** 策略，仅放行 80（HTTP）和 443（HTTPS）等标准端口。5000 属于非标准端口，其出站 TCP 流量在防火墙处被直接丢弃，TCP 握手无法建立。

**解决方案**

在服务器上部署 Nginx 反向代理，监听标准 80 端口，将流量转发到内部 5000：

```
校园网浏览器 → :80 (Nginx) → :5000 (Gunicorn)
              ↑ 80 端口不被拦        ↑ 内部转发不受限制
```

Nginx 配置关键点：

```nginx
server {
    listen 80 default_server;
    server_name _;

    client_max_body_size 20m;

    location / {
        proxy_pass http://127.0.0.1:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # WebSocket 支持（PK 对战依赖）
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 86400s;
    }
}
```

**部署步骤**

| 步骤 | 操作 | 验证 |
|---|---|---|
| 1 | 腾讯云控制台新增 TCP 80 入站规则 | 安全组列表可见 |
| 2 | `sudo apt install -y nginx` | `nginx -v` 输出版本 |
| 3 | 写入 `/etc/nginx/sites-available/kemu1` | — |
| 4 | 软链接到 `sites-enabled`，删除默认站点 | — |
| 5 | `sudo nginx -t` | 输出 `test is successful` |
| 6 | `sudo systemctl restart nginx` | status 显示 active (running) |
| 7 | 校园网内访问 `http://134.175.20.95/` | 页面正常加载 |

---

### 问题 2：Nginx 反代后 WebSocket 连接失败 + 页面卡顿

**问题现象**

Nginx 反代部署后，浏览器 F12 控制台持续报 `WebSocket connection to ... failed`。Socket.io 自动降级为长轮询（polling），每个"实时"消息都变成定时 GET/POST 轮询，体感非常卡。

**分析过程**

排查过程中踩了多个坑，逐步缩小范围：

| 步骤 | 操作 | 结果 | 结论 |
|---|---|---|---|
| 1 | 浏览器 F12 → 控制台 | `WebSocket connection failed` | WebSocket 握手没成功 |
| 2 | gunicorn access log | 全是 `transport=polling`，一条 websocket 都没有 | Socket.io 一直在降级 |
| 3 | curl 直连 5000 测 WebSocket 升级 | `HTTP/1.1 200 OK`（不是 101） | Flask-SocketIO + 普通 gevent worker 不处理升级 |
| 4 | 换 GeventWebSocketWorker + curl 带 Connection 头 | **`101 Switching Protocols` ✅** | GeventWebSocketWorker 才能处理 WebSocket 升级 |
| 5 | 用 GeventWebSocketWorker 后，浏览器刷新 | **还是报 WebSocket failed** | Nginx 配置有问题 |
| 6 | Nginx 配置硬编码 `Connection "upgrade"` | polling 请求也带 upgrade 头，搞乱后端 | 应该用 map 条件转发 |

**根因（三层叠加）**

**第一层：Gunicorn worker class 选错了**

Flask-SocketIO + `gevent` 协程 worker 只处理普通 HTTP 请求，不会处理 WebSocket 协议升级。必须用 `geventwebsocket.gunicorn.workers.GeventWebSocketWorker`——这是 gevent-websocket 包提供的专门支持 WebSocket 的 worker。

**第二层：Nginx 硬编码 Connection 头搞乱 polling 请求**

```nginx
# ❌ 错误写法：给所有请求都加 Connection: upgrade
proxy_set_header Connection "upgrade";
```

这行代码让每一个请求（包括 Socket.io 的 polling GET/POST）都带上 `Connection: upgrade`，可能搞乱 Flask-SocketIO 的内部状态，导致后续 WebSocket 升级流程出问题。

**第三层：curl 测试工具本身的坑**

curl 默认会自动加 `Connection: keep-alive` 请求头，会覆盖掉显式指定的 `Connection: Upgrade`。测试 WebSocket 握手时必须同时显式加 Upgrade 和 Connection 头，或者用 `--http1.0`（HTTP/1.0 没有默认 Connection 头）。

**解决方案**

**1. Gunicorn 用 GeventWebSocketWorker**

```bash
pip install gevent-websocket simple-websocket
gunicorn --worker-class geventwebsocket.gunicorn.workers.GeventWebSocketWorker -w 1 \
  --bind 0.0.0.0:5000 main:app
```

| 包 | 作用 |
|---|---|
| gevent-websocket | 提供 GeventWebSocketWorker，处理 WebSocket 协议 |
| simple-websocket | Flask-SocketIO 底层 WebSocket 实现（必须装） |

**2. Nginx 用 map 条件转发 Connection 头**

```nginx
map $http_upgrade $connection_upgrade {
    default upgrade;    # 有 Upgrade 头 → 也发 Connection: upgrade
    ''      close;       # 没有 Upgrade 头 → 发 Connection: close（普通 HTTP）
}

server {
    listen 80 default_server;
    location / {
        proxy_pass http://127.0.0.1:5000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;  # 条件转发！
        proxy_read_timeout 86400s;
    }
}
```

**3. curl 正确测试 WebSocket 握手**

```bash
# 必须显式同时加 Upgrade 和 Connection 头
curl -i --http1.1 \
  -H "Upgrade: websocket" \
  -H "Connection: Upgrade" \
  -H "Sec-WebSocket-Version: 13" \
  -H "Sec-WebSocket-Key: dGhlIHNhbXBsZSBub25jZQ==" \
  http://127.0.0.1:5000/socket.io/?EIO=4&transport=websocket
# 预期：HTTP/1.1 101 Switching Protocols
```

**验证成功的标志**

- 浏览器 F12 → Network 面板出现 WebSocket 连接
- gunicorn access log 出现 `transport=websocket` 请求
- PK 对战实时同步正常，不降级 polling
- 模拟考试、错题本、通知等所有功能正常工作

---

### 问题 4：腾讯云服务器拉取 GitHub 代码超时

**问题现象**

在服务器上执行 `git pull` 拉取最新代码时，经常卡住数分钟后报错 `fatal: unable to access ... TLS connection -110`。反复重试偶尔成功，但极不稳定。

**分析过程**

腾讯云广州机房到 GitHub 的网络链路不稳定，TLS 握手频繁超时。这是国内服务器访问 GitHub 的常见问题，与代码仓库本身无关。

**解决方案**

在 `deploy/update.sh` 中内置 GitHub 镜像加速，直连失败时自动切换镜像重试：

| 优先级 | 镜像地址 | 命令 |
|---|---|---|
| 1（直连） | github.com | `git pull origin main` |
| 2（备用） | gitclone.com | `git pull https://gitclone.com/github.com/ming279/kemu1_exam.git main` |
| 3（手动） | ghfast.top | `git pull https://ghfast.top/https://github.com/ming279/kemu1_exam.git main` |
| 4（手动） | mirror.ghproxy.com | `git pull https://mirror.ghproxy.com/https://github.com/ming279/kemu1_exam.git main` |

`update.sh` 中的实现：

```bash
$RUN git pull || {
  echo "GitHub 直连失败，改用 gitclone.com 镜像重试 ..."
  $RUN git pull https://gitclone.com/github.com/ming279/kemu1_exam.git main
}
```

---

### 问题 5：root 执行 git pull 导致属主污染，后续更新报权限错误

**问题现象**

部署脚本 `update.sh` 以 root 身份运行，git pull 拉取代码后 `.git/objects` 目录下的文件属主变成 root。之后用 ubuntu 用户手动执行 `git pull` 时报 `insufficient permission` 错误。

**分析过程**

Git 在拉取代码时会在 `.git/objects` 等目录下创建新文件，这些文件继承当前执行用户的属主。root 执行后产生 root 属主文件，普通用户 ubuntu 无权读写，导致后续 git 操作失败。

**解决方案**

在 `update.sh` 中做两层防护：

```bash
# 1. 修复历史残留的 root 属主对象
chown -R ubuntu:ubuntu "$APP_DIR/.git"

# 2. 降权执行：以 ubuntu 用户身份运行 git/pip
RUN="sudo -H -u ubuntu"
$RUN git pull ...
```

这样每次更新时先修复属主，再以 ubuntu 身份执行 git 操作，不再产生 root 属主文件。

---

### 问题 6：数据库迁移重复执行报 Duplicate column 错误

**问题现象**

功能改进批次新增了多个列（`switch_count`、`time_limit_sec`、`theme` 等），通过 `migration_v3.sql` 执行 ALTER TABLE。首次执行成功，但后续每次部署重新执行时都报 `Duplicate column name` 错误并中断。

**分析过程**

MySQL 8.0 的 `ALTER TABLE ADD COLUMN` 不支持 `IF NOT EXISTS` 语法（与 PostgreSQL 不同），重复添加已存在的列会直接报错。迁移脚本中包含多条 ALTER 语句，第一条报错后后续语句不会执行。

**解决方案**

使用 `--force` 参数执行迁移，遇到错误不中断，继续执行后续语句：

```bash
sudo mysql --force kemu1_exam < sql/migration_v3.sql
```

| 参数 | 作用 |
|---|---|
| `--force` | 即使遇到 SQL 错误（如 Duplicate column）也不中断，继续执行下一条语句 |

对于已存在的列，ALTER 报 Duplicate column 错误被忽略；对于新表（CREATE TABLE），脚本使用 `CREATE TABLE IF NOT EXISTS` 避免冲突。这样迁移脚本可以安全地重复执行。

---

### 问题 7：MySQL 保留字 `option` 导致查询报错

**问题现象**

相似题功能开发时，后端查询题目选项表报 `Table 'kemu1_exam.question_option' doesn't exist`。但数据库中确实存在这张表——只是表名是 `option` 而非 `question_option`。

**分析过程**

数据库中存储题目选项的表名为 `option`，这是 MySQL 的保留关键字。在 SQL 中直接使用 `SELECT * FROM option` 会被 MySQL 解析为语法错误，必须用反引号包裹。

**解决方案**

将所有 SQL 中的 `option` 改为 `` `option` ``（加反引号）：

```sql
-- 错误写法
SELECT * FROM option WHERE question_id = %s

-- 正确写法
SELECT * FROM `option` WHERE question_id = %s
```

全局搜索代码中所有引用该表的位置，统一改为反引号写法。

---

### 问题 8：删除试卷时 task_record 外键约束报错

**问题现象**

实现"学生删除已完成试卷"功能时，执行 `DELETE FROM exam_paper WHERE id=<pid>` 报外键约束错误：`Cannot delete or update a parent row: a foreign key constraint fails`。

**分析过程**

数据库外键关系如下：

```
exam_paper (主表)
  ├── exam_detail          ON DELETE CASCADE  ← 自动级联删除，无需处理
  └── task_record          无 CASCADE, 可 NULL ← 需要手动处理
```

`task_record` 表的 `paper_id` 列有外键引用 `exam_paper.id`，但没有设置 `ON DELETE CASCADE`。直接删除 exam_paper 时，如果存在引用该试卷的 task_record 记录，MySQL 会阻止删除以保护数据完整性。

**解决方案**

删除 exam_paper 之前，先将 task_record 中的引用置为 NULL：

```python
# 1. 清除 task_record 悬空引用
execute("UPDATE task_record SET paper_id=NULL WHERE paper_id=%s", (pid,))
# 2. 删除试卷主表（exam_detail 由 CASCADE 自动删除）
execute("DELETE FROM exam_paper WHERE id=%s", (pid,))
```

| 步骤 | SQL | 目的 |
|---|---|---|
| 1 | `UPDATE task_record SET paper_id=NULL WHERE paper_id=<pid>` | 解除外键引用 |
| 2 | `DELETE FROM exam_paper WHERE id=<pid>` | 删主表，exam_detail 由 CASCADE 自动级联删除 |

---

### 问题 9：PK 对战 Socket.io 连接假死，对方准备后无法开局

**问题现象**

PK 对战中，玩家 A 点击"准备"后，玩家 B 的页面上看不到 A 的准备状态，点击"开始对战"无反应。刷新页面后偶尔恢复正常，但极不稳定。问题在长时间挂机后更容易出现。

**分析过程**

Flask-SocketIO 通过 sid（Session ID）标识每个连接。服务器端在 `pk_join` 事件中记录玩家的 sid 到 `room_state` 字典。但 Socket.io 的底层连接在闲置一段时间后可能被中间网络设备（NAT、代理）静默断开，客户端会自动重连并获得新的 sid，而服务器端仍持有旧 sid——导致后续的 emit 推送到旧 sid，客户端收不到。

**根因**

服务器端的 `room_state` 字典中的 sid 与客户端实际连接的 sid 不一致（客户端重连后 sid 变了，但服务器不知道）。

**解决方案**

客户端心跳机制：每 8 秒重新 emit 一次 `pk_join` 事件，让服务器始终持有最新的 sid：

```javascript
// 客户端：每 8 秒重新 join，让服务端持有最新 sid
setInterval(function() {
  if (socket.connected && !gameStarted) {
    socket.emit('pk_join', { room_key: ROOM_KEY });
  }
}, 8000);
```

服务端在 `pk_join` 事件中更新 `room_state` 中的 sid，并在每次收到 `pk_join` 时检查是否满足开局条件（双方 ready + 双方在线），如果满足则自动触发开局。

| 改进点 | 说明 |
|---|---|
| 客户端心跳 | 每 8s 重新 emit `pk_join`，确保服务端 sid 最新 |
| 开局条件检查 | 每次 `pk_join` 时检查双方是否 ready + 双方 sid 是否有效 |
| disconnect 不清 ready | 断线不重置准备状态，重连后保持，避免需重新点准备 |
| 心跳仅等待阶段运行 | 游戏开始后停止心跳，避免 room_state 广播抖动 |

---

### 问题 10：Gunicorn worker class 选择与 WebSocket 兼容性

**问题现象**

部署时按照网上教程配置 `gunicorn --worker-class eventlet` 启动失败；换成普通 `gevent` worker 后，HTTP 请求正常但 WebSocket 无法升级（Socket.io 一直降级 polling）；换成 `GeventWebSocketWorker` 后 WebSocket 正常，但此前 Nginx 硬编码 Connection 头导致 HTTP 请求异常。

**分析过程**

Gunicorn 支持多种 worker class，但不是每个都支持 WebSocket：

| worker class | WebSocket | HTTP 请求 | 备注 |
|---|---|---|---|
| `eventlet` | ✅ | ✅ | Gunicorn v23+ 已移除内置支持，需装 eventlet |
| `gevent`（普通） | ❌ | ✅ | 只处理普通 HTTP，不处理 Upgrade 协议升级 |
| `geventwebsocket.gunicorn.workers.GeventWebSocketWorker` | ✅ | ✅ | gevent-websocket 包提供，正确支持 WebSocket |

项目代码早期注释写的"服务器装了 eventlet 自动用 eventlet"是误区——Gunicorn 的 worker class 是**显式指定**的，不存在自动检测。

**解决方案**

使用 `GeventWebSocketWorker`，这是 Flask-SocketIO + Gevent 组合的正确搭配：

```bash
# 安装必要依赖
pip install gevent-websocket simple-websocket

# systemd service 中的 ExecStart
gunicorn --worker-class geventwebsocket.gunicorn.workers.GeventWebSocketWorker \
  -w 1 --bind 0.0.0.0:5000 --timeout 120 main:app
```

| 配置 | 值 | 理由 |
|---|---|---|
| `GeventWebSocketWorker` | 专用 WebSocket worker | Flask-SocketIO + gevent 正确搭配 |
| `-w 1` | 单 worker | 多 worker 下 Socket.io 房间状态不共享 |
| `simple-websocket` | Flask-SocketIO 底层 | 必须装，否则 Flask-SocketIO 无法处理 WebSocket |

**关键教训**

GeventWebSocketWorker 能正确处理 WebSocket，但必须配合 Nginx 的 **map 条件转发 Connection 头**。如果 Nginx 硬编码 `Connection "upgrade"` 给所有请求，polling 请求也会带 upgrade 头，可能导致 Flask-SocketIO 内部状态混乱。

---

### 问题 11：Lightbox 图片放大跟随浏览器 Ctrl+/- 缩放

**问题现象**

实现了点击题目图片全屏放大的 Lightbox 功能后，发现一个视觉问题：当用户用浏览器 Ctrl+ 或 Ctrl- 调整页面缩放比例时，Lightbox 放大的图片也会跟着放大/缩小，导致图片大小不受控。

**分析过程**

Lightbox 使用 CSS `transform: scale()` 或固定像素值实现放大。但浏览器的 Ctrl+/- 缩放会改变页面的有效 DPR（设备像素比），所有用 px/calc/transform 定义的尺寸都会被浏览器按缩放比例重新计算，导致 Lightbox 图片跟随缩放。

**解决方案**

通过一个 1000px 的隐藏探针元素测量浏览器的实际缩放比例，然后用反向 scale 抵消：

```javascript
// 隐藏探针：1000px 宽，测量浏览器缩放后的实际渲染宽度
var probe = document.createElement('div');
probe.style.cssText = 'position:absolute;width:1000px;height:0;visibility:hidden;';
document.body.appendChild(probe);
var zoom = probe.offsetWidth / 1000;  // 缩放比例（如 1.25 = 125%）

// Lightbox 图片用反向 scale 抵消浏览器缩放
img.style.transform = 'scale(' + (1 / zoom) + ')';
```

| 步骤 | 操作 | 目的 |
|---|---|---|
| 1 | 创建 1000px 隐藏探针元素 | 作为缩放比例的参照 |
| 2 | `probe.offsetWidth / 1000` 得到缩放比例 | 如浏览器放大到 125%，探针渲染为 1250px，比例为 1.25 |
| 3 | 图片用 `scale(1/zoom)` 反向抵消 | 1/1.25=0.8，抵消浏览器的 1.25 倍放大 |

---

### 问题 12：答题数据导出多选时 SQL 注入风险

**问题现象**

导出功能从单选改为复选框多选后，后端需要将多个任务 ID / 学生 ID 拼接成 SQL 的 `IN (...)` 子句。直接字符串拼接 `",".join(ids)` 存在 SQL 注入风险。

**分析过程**

多选场景下，前端提交的是 ID 列表（如 `task_ids=[1, 3, 5]`），后端需要构造：

```sql
WHERE task_id IN (1, 3, 5)
```

如果直接用字符串拼接，恶意输入可能注入 SQL。必须使用参数化查询，但 `IN` 子句的占位符数量是动态的。

**解决方案**

封装 `_in_clause()` 工具函数，生成参数化的 IN 子句：

```python
def _in_clause(ids):
    """生成 IN (...) 子句的 SQL 片段和参数"""
    if not ids:
        return ("(0)", ())  # 空列表返回不可能匹配的值
    placeholders = ",".join(["%s"] * len(ids))
    return (f"IN ({placeholders})", tuple(ids))

# 使用
task_clause, task_params = _in_clause(task_ids)
sql = f"SELECT * FROM task_record WHERE task_id {task_clause}"
rows = q(sql, task_params)
```

这样无论用户选了多少个 ID，SQL 语句都是参数化的，不会有注入风险。

---

## 三、常用运维命令

| 场景 | 命令 |
|---|---|
| 查看服务状态 | `systemctl status kemu1` |
| 重启服务 | `systemctl restart kemu1` |
| 查看实时日志 | `journalctl -u kemu1 -f` |
| 查看最近 50 行日志 | `journalctl -u kemu1 -n 50` |
| 重启 Nginx | `sudo systemctl restart nginx` |
| 查看端口监听 | `ss -tlnp \| grep -E '5000\|80\|3306'` |
| 进入 MySQL | `sudo mysql kemu1_exam` |
| 执行数据库迁移 | `sudo mysql --force kemu1_exam < sql/migration_v3.sql` |
| 更新代码 | `sudo bash deploy/update.sh` |
| 健康检查 | `curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:5000/login` |

---

## 四、部署后冒烟测试流程

每次部署后执行的功能验证：

| 序号 | 测试项 | 操作步骤 | 预期结果 |
|---|---|---|---|
| 1 | 服务可用性 | `curl http://127.0.0.1/login` | HTTP 200 |
| 2 | 管理员登录 | admin/admin123 登录 | 跳转首页 |
| 3 | 学生登录 | 123456/123456 登录 | 跳转首页 |
| 4 | 顺序练习 | 选择题型 → 开始练习 | 题目加载正常 |
| 5 | 模拟考试 | 导航栏 → 限时 45 分钟 | 倒计时正常 |
| 6 | 试卷删除 | my_papers → 点删除 → confirm | 行消失 |
| 7 | PK 对战 | 创建房间 → 双方准备 → 开局 | WebSocket 正常 |
| 8 | 错题本 | 查看错题 → 相似题 | 页面正常 |
| 9 | 图片放大 | 点击题目图片 | Lightbox 全屏 |
| 10 | 数据导出 | admin → 复选框多选 → 导出 | 文件下载 |
| 11 | 站内通知 | admin → 发送通知 | 学生端红点推送 |
| 12 | 外网访问 | 校园网访问 `http://134.175.20.95/` | 页面正常加载 |

---

## 五、问题汇总表

| 序号 | 问题 | 根因 | 解决方案 |
|---|---|---|---|
| 1 | 校园网无法访问 :5000 | 防火墙拦截非标准端口 | Nginx 反代到 80 端口 |
| 2 | WebSocket 连接失败 + 页面卡顿 | 三层叠加：worker class 选错 + Nginx 硬编码 Connection 头 + curl 测试误判 | GeventWebSocketWorker + Nginx map 条件转发 + simple-websocket |
| 3 | git pull 超时 | 腾讯云连 GitHub 不稳定 | 镜像加速（gitclone.com / ghfast.top） |
| 4 | git 属主污染 | root 执行 git 产生 root 属主文件 | chown 修复 + 降权执行 |
| 5 | migration 报 Duplicate column | MySQL 8 不支持 IF NOT EXISTS | `mysql --force` 忽略错误 |
| 6 | `question_option` 表不存在 | 表名 `option` 是 MySQL 保留字 | SQL 加反引号 `` `option` `` |
| 7 | 删试卷外键报错 | task_record 无 ON DELETE CASCADE | 删前先 `SET paper_id=NULL` |
| 8 | PK 假死无法开局 | 客户端重连后 sid 变化 | 8s 心跳重新 emit pk_join |
| 9 | Lightbox 跟随浏览器缩放 | CSS px 受浏览器 Ctrl+/- 影响 | 反向 scale 抵消 |
| 10 | 导出多选 SQL 注入风险 | IN 子句直接字符串拼接 | `_in_clause()` 参数化查询 |
